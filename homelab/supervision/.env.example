# → copier en .env (généré automatiquement par create-lxc-supervision.sh)
GRAFANA_ADMIN_PASSWORD=CHANGEME
# Secret du webhook L5 (GRAFANA_WEBHOOK_SECRET / SUPERVISION_WEBHOOK_SECRET dans /opt/l5/.env du CT L5)
SUPERVISION_WEBHOOK_SECRET=CHANGEME
